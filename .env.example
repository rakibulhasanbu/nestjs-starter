# Application
NODE_ENV=development
PORT=3000
APP_URL=http://localhost:3000

# CORS: comma-separated allowed origins. Required in production; if unset in
# development/test, all origins are allowed.
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Database (PostgreSQL)
# Format: postgresql://<user>:<password>@<host>:<port>/<database>?schema=public
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/nest_starter?schema=public"

# Redis — used as the shared L2 cache for resolved permissions (see AuthorizationModule).
# The app degrades to direct DB reads if Redis is unreachable; it never hard-fails on it.
REDIS_URL="redis://localhost:6379"

# Auth
JWT_ACCESS_SECRET=change-me-to-a-random-32-byte-hex-string
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=30d

EMAIL_VERIFICATION_TTL_MINUTES=5
PASSWORD_RESET_TTL_MINUTES=5
DELETE_ACCOUNT_OTP_TTL_MINUTES=5

LOGIN_MAX_ATTEMPTS=5
LOGIN_LOCKOUT_MINUTES=15

DELETED_USER_GRACE_DAYS=15

# Google Sign-In (OAuth2 client ID from Google Cloud Console)
GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com

# WebAuthn / Passkeys
WEBAUTHN_RP_ID=localhost
WEBAUTHN_RP_NAME=Nest Starter
WEBAUTHN_ORIGIN=http://localhost:3000
WEBAUTHN_CHALLENGE_TTL_MINUTES=5

# 2FA (TOTP) — TWO_FACTOR_ENCRYPTION_KEY must be a random 32-byte hex string (64 chars)
TWO_FACTOR_APP_NAME=Nest Starter
TWO_FACTOR_ENCRYPTION_KEY=change-me-to-a-random-64-char-hex-string-0000000000000000000000
TWO_FACTOR_LOGIN_TTL=5m

# Permission cache tuning
# L1 = per-process memory, L2 = Redis. Both have TTLs so stale data self-heals
# even if an explicit invalidation is ever missed.
PERM_CACHE_L1_TTL_MS=30000
PERM_CACHE_L2_TTL_SECONDS=300
PERM_CACHE_L1_MAX_ENTRIES=10000

# Seed-only super admin bootstrap (see prisma/seed.ts) — not usable to create further admins
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me-please-1234
